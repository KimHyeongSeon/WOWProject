#include "stdlib.h"
#include "stdafx.h"

int _tmain(int argc, _TCHAR* argv[])
{   
int num;
int i=9;
printf("역순으로 출력할 단 입력 : ");
scanf("%d", &num);
while(i>0) 
{  printf("%dX%d=%d \n", num, i, num*i); 
i--; } 
system("pause"); 
return 0;}
