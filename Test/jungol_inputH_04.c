#include "stdafx.h"
#include "stdlib.h"


int main()
{
	int x, y, z;

	scanf("%d%d%d", &x, &y, &z);

	printf("합 = %d\n", x + y + z);
	printf("평균 = %d", (x + y + z) / 3);


	system("pause");
	return 0;
}

